---
# Packages from shared_packages that must be installed as Homebrew casks on macOS
homebrew_cask_packages:
  - kitty
  - helium
  - autodesk-fusion
  - affinity

# Packages with different names on macOS (Homebrew)
package_name_map:
  git-delta:
    darwin: delta
  python3:
    darwin: python3
  fd:
    darwin: fd
  gh:
    darwin: gh
